services:
  - type: web
    name: ai-question-generator
    runtime: python
    repo: https://github.com/anishku2102/product-transparency-assignment
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
